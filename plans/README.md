# Implementation Plans

Generated by the improve skill on 2026-06-18, against commit `c37464c`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add a CLAUDE.md capturing this repo's agent-session gotchas | P2 | S | — | DONE |
| 002  | Add an RSS feed endpoint at `GET /feed.xml` | P3 | S–M | — | DONE |
| 003  | Add a clickable domain filter to `/api/stories` and the frontend | P3 | S–M | — | DONE |

## Dependency notes

- None of these three plans depend on each other and can be executed in any
  order, or in parallel by separate executors. 001 is listed first only
  because it's the cheapest and highest-priority (P2 vs P3 for the other two).
- If plan 001's CLAUDE.md lands first, plans 002 and 003's executors should
  read it before starting — it documents the D1 100-bound-parameter cap and
  the `pnpm run deploy` (not bare `pnpm deploy`) caveat, both relevant to
  002 and 003's steps.

## Findings considered and rejected

- **Fractional/negative `page` query param** (e.g. `?page=1.5`, `?page=0`,
  `?page=-3`, `?page=abc`): tested live against the deployed API on
  2026-06-18 — all degrade safely to page-1-equivalent behavior, no error.
  Not a bug, no plan written.
- **Transitive dev-dependency advisories** (`ws` high via
  `wrangler>miniflare>ws`, `esbuild` low via `wrangler>esbuild`, found via
  `pnpm audit` on 2026-06-18): real advisories, but wrangler is already at
  latest (4.101.0) and both only affect the local `wrangler dev` tooling,
  never the deployed Worker. Nothing actionable today — re-run `pnpm audit`
  after the next wrangler version bump rather than planning a fix now.
- **No CI pipeline** (typecheck-on-push workflow): identified in the audit
  as finding DX-01, P-ranked above the CLAUDE.md finding, but the user did
  not select it for a plan this round. Not rejected — just not yet planned;
  reconsider next pass.
- **Per-request threshold override** (`?minPoints=`, `?days=` on
  `/api/stories`): identified as a direction option in the audit, not
  selected for a plan this round. Not rejected — reconsider next pass.

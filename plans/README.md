# Implementation Plans

Generated by the improve skill on 2026-06-18, against commit `c37464c`.
Updated 2026-06-21, against commit `26117fd`, with plans 004-006.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add a CLAUDE.md capturing this repo's agent-session gotchas | P2 | S | — | DONE |
| 002  | Add an RSS feed endpoint at `GET /feed.xml` | P3 | S–M | — | DONE |
| 003  | Add a clickable domain filter to `/api/stories` and the frontend | P3 | S–M | — | DONE |
| 004  | Escape LIKE metacharacters in the `/api/stories` domain filter | P2 | S | — | DONE |
| 005  | Document `/feed.xml` and the `domain` filter in README | P3 | S | — | DONE |
| 006  | Deduplicate the story-row type between `/api/stories` and `/feed.xml` | P3 | S | — | DONE |

## Incident reports

- **007** — [Poller outage: Algolia dropped `points` as a filterable
  attribute](./007-poller-outage-rca.md). Not an implementation plan (it's
  a retrospective on an already-resolved 2026-07-08/09 outage), so it's
  excluded from the execution-order table above.

## Dependency notes

- Plans 001-003 don't depend on each other and were executed independently.
- Plans 004, 005, and 006 are also independent of each other and of
  001-003, and can run in any order or in parallel by separate executors.
  004 is listed first only because it's a real bug fix (P2) vs. the other
  two being polish (P3).
- 005 documents the `domain` filter's request/response shape, which is
  unaffected by 004's fix (004 changes what an edge-case input does
  server-side, not the documented contract) — no ordering requirement
  between them.
- 006 touches the same handlers as 004 (both edit `src/index.ts`'s
  `/api/stories` function) but in non-overlapping ways (004: the `WHERE`
  clause and bind call; 006: the type annotation). If both are executed by
  separate agents in parallel, expect a small merge conflict around the
  `/api/stories` handler — not a logical conflict, just adjacent edits.

All three executed and reviewed on 2026-06-21 (each in an isolated
worktree, none merged — see below for branches/commits):

- **004** (`advisor/004-domain-filter-like-escaping`, commit `b6b7bcf`):
  the plan's literal `ESCAPE '\'` snippet was a JS template-literal
  transcription bug — `\'` collapses to `'` inside a template literal, so
  the SQL received an empty escape character. The executor caught this via
  D1's `ESCAPE expression must be a single character` error and corrected
  it to `ESCAPE '\\'` in all 8 clauses (yielding the correct single
  backslash at runtime). Verified independently on re-review: all three
  `curl` checks (normal filter, literal `%`, literal `_`) behave exactly
  as specified.
- **005** (branch `worktree-agent-a69a895e5a3a28deb`, commit `6f4646f`):
  executor's harness assigned its own worktree branch name instead of the
  plan's suggested `advisor/005-readme-endpoint-docs`; content matches the
  plan exactly, re-verified against the live site.
- **006** (`advisor/006-dedupe-story-row-type`, commit `bc10fb2`): clean,
  matches the plan exactly. Executor additionally ran
  `pnpm run schema:local` in its fresh worktree (not a plan step, but the
  documented `CLAUDE.md` bootstrap procedure) to get a working local DB for
  the runtime smoke check — reasonable, touched no tracked files.

## Findings considered and rejected

- **Fractional/negative `page` query param** (e.g. `?page=1.5`, `?page=0`,
  `?page=-3`, `?page=abc`): tested live against the deployed API on
  2026-06-18 — all degrade safely to page-1-equivalent behavior, no error.
  Not a bug, no plan written.
- **Transitive dev-dependency advisories** (`ws`/`undici` high+moderate via
  `wrangler>miniflare`, `esbuild` low via `wrangler>esbuild`): re-checked on
  2026-06-21 (installed wrangler 4.101.0, latest 4.103.0 — one minor
  version behind). Same conclusion as before: all advisories are in
  dev-only tooling paths (`wrangler dev`/`deploy`), never the deployed
  Worker. Nothing actionable today; re-run `pnpm audit` after the next
  wrangler version bump rather than planning a fix now.
- **No CI pipeline** (typecheck-on-push workflow): raised in the 2026-06-18
  audit (DX-01) and again in the 2026-06-21 audit (finding #3). Not
  selected for a plan either time. Not rejected — just deferred twice now;
  worth prioritizing next pass if it comes up a third time.
- **Per-request threshold override** (`?minPoints=`, `?days=` on
  `/api/stories`): identified as a direction option in both the 2026-06-18
  and 2026-06-21 audits, not selected for a plan either time. Not
  rejected — reconsider next pass.
- **No automated test coverage** (typecheck is the only gate; no tests
  exist for `poll()`'s dedup logic, pagination, or the domain filter —
  finding #2 in the 2026-06-21 audit, which is exactly the class of bug
  plan 004 fixes): not selected for a plan this round. Not rejected —
  reconsider next pass, especially if another untested-logic bug like 004
  surfaces.
- **Infinite scroll instead of the Newer/Older pager** (direction option,
  2026-06-21 audit): the current pager is simple and already CLS-safe
  (see the web-perf work earlier in this session); not an obvious win, not
  selected.
